 <tr>
  <th scope="row "><%= stt %></th>
  <td><%= product.title %></td>
  <td><%= product.product_type.upcase %></td>
  <td><%= product.quantity + product_sold_quantity(product) %></td>
  <td><%= product.quantity %></td>   
  <td><%= product_sold_quantity(product) %></td>
 </tr>